services:
  kort:
    build:
      context: .
      args:
        NEXT_PUBLIC_BRAND: ${NEXT_PUBLIC_BRAND:-mahoje}
    environment:
      - NODE_ENV=production
      - NEXT_PUBLIC_BRAND=${NEXT_PUBLIC_BRAND:-mahoje}
    restart: unless-stopped
    expose:
      - "3000"
